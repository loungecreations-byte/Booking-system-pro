const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.goto('http://dagjedenbosch.local/product/e-chopper-tour/'); const bodyText = await page.textContent('body'); console.log('test222: ' + bodyText.includes('test222')); console.log('test111: ' + bodyText.includes('test111')); await browser.close(); })();